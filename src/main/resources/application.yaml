spring:
  application:
    name: conflicttracker
  profiles:
    active: prod  # Ahora coincide con 'application-prod.yml'

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true